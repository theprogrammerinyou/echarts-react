import React from 'react';
import ReactDOM from 'react-dom';

import App from './App';

// tells react to render App component and add it to the element with id root to the DOM. 
ReactDOM.render(<App />, document.getElementById('root'));